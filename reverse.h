#include <string>
#include <vector>
#include <iostream>
using namespace std;

int main)()
{
	vector<string> words;
	string word;
	
	while(cin>>word)
	{
		words.push_back(word);
		{
			if (word=="done")
				break;
		}
		words.puch_back(word);
	}
	while (words.puch_back()>0)
	{
		cout<<words.back()<<endl;
		words.pop_back()<<endl;
		words.pop_back();
	}
	return 0;
}